---
phase: quick-fix
plan: d57e
type: execute
wave: 1
depends_on: []
files_modified:
  - service/frontend/src/components/TestForm.jsx
autonomous: true
requirements: []
user_setup: []
must_haves:
  truths:
    - "User can add test steps when creating a new test"
    - "User can edit test steps when editing an existing test"
    - "User can remove test steps from the test form"
    - "Test steps are saved to the database when test is created/updated"
  artifacts:
    - path: "service/frontend/src/components/TestForm.jsx"
      provides: "Test creation/editing form with step configuration"
      contains: "StepEditor component integration"
  key_links:
    - from: "TestForm.jsx"
      to: "StepEditor.jsx"
      via: "import statement"
      pattern: "import StepEditor from './StepEditor'"
    - from: "StepEditor"
      to: "formData.test_steps"
      via: "onChange callback"
      pattern: "onChange.*test_steps"
---

<objective>
Enable test step configuration in the test management form

Purpose: Users need to be able to add, edit, and remove test steps when creating or editing test definitions. The StepEditor component exists but is commented out in TestForm.jsx.

Output: Functional test step configuration UI in the test management page
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@service/frontend/src/components/TestForm.jsx
@service/frontend/src/components/StepEditor.jsx
@service/frontend/src/api.js
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enable StepEditor component in TestForm</name>
  <files>service/frontend/src/components/TestForm.jsx</files>
  <action>
    Uncomment the StepEditor import (line 4) and remove the temporary comment block around the StepEditor usage (lines 405-407).

    Specifically:
    1. Uncomment line 4: `import StepEditor from './StepEditor';`
    2. Remove lines 405-407 (the comment block explaining why StepEditor was disabled)
    3. Replace the comment with the actual StepEditor component:
       ```jsx
       <StepEditor steps={formData.test_steps} onChange={(steps) => setFormData({...formData, test_steps: steps})} />
       ```

    The StepEditor component is already implemented and functional - it was just temporarily disabled due to a "loading issue" that has since been resolved.
  </action>
  <verify>
    <automated>grep -c "StepEditor" service/frontend/src/components/TestForm.jsx | grep -q "^2$" && echo "StepEditor import and usage found"</automated>
  </verify>
  <done>
    StepEditor component is imported and rendered in TestForm
    Users can add, edit, and remove test steps when creating/editing tests
    Test steps are properly saved via the existing API integration
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → API | User input crosses here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-d57e-01 | I | TestForm step input | accept | Low risk - steps are part of test definition, validated by backend API schema |
| T-d57e-02 | T | StepEditor onChange | accept | No sensitive data - step descriptions are natural language test instructions |
</threat_model>

<verification>
- StepEditor component renders in TestForm
- Users can add multiple steps with the "+ Add Step" button
- Users can edit step descriptions in textarea fields
- Users can remove steps with the "Remove" button
- Steps are numbered sequentially (1, 2, 3...)
- Form submission includes test_steps in the payload
- API saves steps to database via /api/v1/test-steps/test-definition/{id}
</verification>

<success_criteria>
- TestForm renders StepEditor component without errors
- Creating a new test with steps succeeds
- Editing an existing test shows its existing steps
- Adding/removing steps in edit mode updates the test correctly
- Steps persist after form submission
</success_criteria>

<output>
After completion, create `.planning/quick/260504-d57e-test-management-page-steps-config/260504-d57e-SUMMARY.md`
</output>
