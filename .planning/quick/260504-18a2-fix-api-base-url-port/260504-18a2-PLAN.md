---
phase: quick
plan: 260504-18a2
type: execute
wave: 1
depends_on: []
files_modified:
  - service/frontend/src/api.js
autonomous: true
requirements: []
user_setup: []
must_haves:
  truths:
    - API calls work when accessing frontend through nginx (localhost:8080)
    - API calls work when accessing Vite dev server directly (localhost:5173)
    - No hardcoded port numbers in API configuration
  artifacts:
    - path: service/frontend/src/api.js
      provides: API base URL configuration
      contains: BASE_URL
  key_links:
    - from: service/frontend/src/api.js
      to: nginx reverse proxy
      via: window.location.origin
      pattern: window.location.origin
---

<objective>
Fix API base URL configuration to work correctly when accessing the frontend through nginx (port 8080) or directly through Vite dev server (port 5173).

Purpose: Ensure API calls work regardless of how the frontend is accessed (nginx proxy or direct Vite dev server)
Output: Robust API base URL configuration that adapts to the access method
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@service/frontend/src/api.js
@service/nginx/nginx.conf

Current architecture:
- Nginx reverse proxy on port 8080 (external) → routes to backend services on internal ports
- Vite dev server on port 5173 (internal Docker network)
- Backend services: 8011 (test-case), 8012 (scheduler), 8013 (dashboard)
- Nginx proxies /api/v1/ to backend services
- Frontend currently uses window.location.origin for BASE_URL
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify and enhance API base URL configuration</name>
  <files>service/frontend/src/api.js</files>
  <action>The current implementation using window.location.origin is correct for nginx access. However, we should add environment-based fallback and logging to ensure it works in all scenarios:

1. Keep window.location.origin as the primary method (already correct)
2. Add console logging to verify BASE_URL at runtime
3. Add a comment explaining why window.location.origin works for both nginx and direct access
4. Ensure all API endpoints use the consistent BASE_URL pattern

Do NOT hardcode port numbers (8080, 5173, etc.) - the dynamic approach is correct.

Key insight: When accessed through nginx at localhost:8080, window.location.origin returns "http://localhost:8080", and nginx proxies /api/v1/ to the backend. This is the correct behavior.</action>
  <verify>grep -c "window.location.origin" service/frontend/src/api.js | grep -q "1" && echo "BASE_URL uses window.location.origin"</verify>
  <done>API configuration works for both nginx (8080) and direct Vite (5173) access with no hardcoded ports</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Nginx | Untrusted client requests |
| Nginx → Backend | Internal Docker network |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-quick-01 | I | API base URL | accept | Low risk - only determines endpoint, no data exposure |
| T-quick-02 | S | window.location.origin | accept | Browser API, read-only, no security impact |
</threat_model>

<verification>
1. Access frontend through nginx at http://localhost:8080
2. Verify API calls succeed (check browser Network tab)
3. Access frontend directly at http://localhost:5173 (if accessible)
4. Verify API calls still work
5. Confirm no hardcoded port numbers in api.js
</verification>

<success_criteria>
- API calls work when accessing through nginx (port 8080)
- No hardcoded port numbers in API configuration
- Console logging shows correct BASE_URL at runtime
- All API endpoints use consistent BASE_URL pattern
</success_criteria>

<output>
After completion, create `.planning/quick/260504-18a2-fix-api-base-url-port/260504-18a2-SUMMARY.md`
</output>
